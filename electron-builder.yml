# electron-builder.yml
# Configuração de empacotamento do Horti Gestão PDV para Windows .exe

appId: "com.hortigestao.pdv"
productName: "Horti Gestão PDV"
copyright: "© 2025 Horti Gestão"

# Diretório de saída dos instaladores
directories:
  output: "C:\\Temp\\horti-build"
  buildResources: "electron"

# Arquivos incluídos no pacote Electron (o mínimo necessário)
files:
  - "electron/main.cjs"
  - "electron/preload.cjs"
  - "package.json"

# Recursos extras copiados para resources/ dentro do instalado
# O servidor Next.js standalone fica em resources/standalone/
extraResources:
  # server.js + package.json (raiz do standalone)
  - from: ".next/standalone"
    to: "standalone"
    filter:
      - "server.js"
      - "package.json"
  # node_modules do standalone (entrada separada — electron-builder exclui por padrão)
  - from: ".next/standalone/node_modules"
    to: "standalone/node_modules"
  # .next/ completo: server/ (páginas compiladas) + static/ (assets)
  - from: ".next/standalone/.next"
    to: "standalone/.next"
  # public/ (imagens, version.json, etc.)
  - from: ".next/standalone/public"
    to: "standalone/public"

# ── Windows ──────────────────────────────────────────────────────────────────
win:
  target:
    - target: nsis
      arch:
        - x64
  icon: electron/icon.ico

nsis:
  oneClick: true                        # instalação silenciosa com 1 clique
  perMachine: false                     # instala por usuário (não precisa admin)
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: "Horti Gestão PDV"
  installerLanguages:
    - pt_BR
  language: "1046"                      # Português Brasil
  artifactName: "HortiGestao-PDV-Setup-${version}.exe"

# ── Publicação (GitHub Releases — opcional para auto-update) ─────────────────
# publish:
#   provider: github
#   owner: seu-usuario
#   repo: horti-gestao
